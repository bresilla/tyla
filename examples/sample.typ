= Introduction

This is a small Typst document used to demonstrate #emph[tyla].

= Mathematics

Euler's identity is $e^(i pi) + 1 = 0$.

The quadratic formula:

$ x = (-b plus.minus sqrt(b^2 - 4 a c)) / (2 a) $
